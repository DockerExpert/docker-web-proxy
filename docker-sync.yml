version: "2"

options:
  verbose: true

syncs:
  docker-mac-sync:
    src: './'
    sync_args:
    - "-prefer newer"
    - "-ignore='Path .git .idea .docker .docker-sync'"
    - "-ignore='BelowPath .git .idea .docker .docker-sync'"